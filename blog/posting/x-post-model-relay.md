Model Relay Pattern:

Proposer -> Critic -> Reconciler -> Executor -> Verifier

One prompt moves through role-specific models before the result is trusted.

It is not just "multi-agent." It is a cleaner handoff pattern for using different LLMs where each is strongest.

Write-up:
https://uldren.ai/blog/model-relay-pattern
