# Uldren Loom Templates

Jinja-compatible template planning for Loom-authored HTML and text. The crate parses templates,
validates Loom host calls, extracts static dependencies, and returns a processing plan. It does not
serve HTTP, expose MCP, or execute programs.
